name: ci
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - master

jobs:
  plugin-ci:
    uses: mattermost/actions-workflows/.github/workflows/plugin-ci.yml@7d90fb64170954e552f79c3145ebaa883da13a9f
    secrets: inherit
    with:
      golang-version: 1.23
      golangci-lint-version: v2.1.1
